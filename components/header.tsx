/**
 * Compatibilidad temporal con las vistas v2. El encabezado y la navegación
 * ahora viven en AppShell, por lo que este componente no renderiza otro header.
 */
export function Header() {
  return null;
}
